/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OrderDto } from '../../models/order-dto';

export interface ApiOrderGet$Json$Params {
}

export function apiOrderGet$Json(http: HttpClient, rootUrl: string, params?: ApiOrderGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<OrderDto>>> {
  const rb = new RequestBuilder(rootUrl, apiOrderGet$Json.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<OrderDto>>;
    })
  );
}

apiOrderGet$Json.PATH = '/api/Order';
